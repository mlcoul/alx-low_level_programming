#include "main.h"
#include <stdio.h>

/**
 *add - write a function that adds two int
 *@a: num to be use
 *@b: num to be use
 *Return: always 0 (Sucess)
 */
int add(int a, int b)
{
	return (a + b);
}
